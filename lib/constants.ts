/** Cookie name for a guest shopper's country preference (Section 4). */
export const GUEST_COUNTRY_COOKIE = "tus_country";

/** Cookie name identifying a guest's cart before they have an account. */
export const GUEST_CART_COOKIE = "tus_cart_session";
