export const metadata = { title: "Track Order" };

export default function TrackorderPage() {
  return (
    <main className="mx-auto flex min-h-[60vh] max-w-2xl flex-col items-center justify-center gap-4 px-6 py-16 text-center">
      <span className="text-xs uppercase tracking-[0.3em] text-brass-400">Track Order</span>
      <p className="max-w-md text-sm text-bone-500">Order tracking arrives in Phase 8 once orders exist.</p>
    </main>
  );
}
